/**
 * Strategy Context module (simplified - no vector search)
 *
 * Retrieves user's strategy content directly and includes it in prompts.
 */

export { retrieveTradingContext, type RetrievalContext } from "./retrieval";
